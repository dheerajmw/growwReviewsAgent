# Streamlit Cloud → Settings → Secrets (TOML)

# Phase 8 BFF on Render (required)
PULSE_API_URL = "https://groww-pulse-api.onrender.com"

# React Pulse 6 UI — same as localhost:5173 (optional; defaults on Streamlit Cloud)
PULSE_UI_URL = "https://groww-pulse-ui.onrender.com"

# Force legacy Python Streamlit widgets instead of React embed
# PULSE_STREAMLIT_NATIVE = false
