# Streamlit Cloud → Settings → Secrets (TOML)

# Phase 8 BFF on Render (required)
PULSE_API_URL = "https://groww-pulse-api.onrender.com"

# Optional: embed React app in iframe (can break styling on some browsers — not recommended)
# PULSE_USE_REACT_UI = false
# PULSE_UI_URL = "https://groww-pulse-ui.onrender.com"
